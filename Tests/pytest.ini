[pytest]

markers =
    regression:Run GUI regression in DEMO
    max:run max regression
    asyncio: mark test to be run with asyncio
    qa: Run GUI regression in QA

    log_level = ERROR

    filterwarnings =
    ignore::DeprecationWarning
    ignore::ResourceWarning